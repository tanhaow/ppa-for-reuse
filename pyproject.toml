[tool.ruff]
line-length = 100
# Avoid noisy line-length errors in generated migration files and long-test-strings
per-file-ignores = { "ppa/*/migrations/*.py" = ["E501"], "ppa/**/tests/*.py" = ["E501"] }

[tool.black]
line-length = 100

 

